{
  "name": "@falina/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Falina shared contracts: types, models, constants, validation for falina-web and falina-api.",
  "license": "UNLICENSED",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "~6.0.3"
  }
}
