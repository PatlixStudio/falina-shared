{
  "name": "falina-shared",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/falina-shared/src",
  "projectType": "library",
  "tags": ["scope:shared"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "libs/falina-shared",
        "command": "npx tsc --noEmit"
      }
    },
    "lint": {
      "executor": "nx:noop"
    },
    "test": {
      "executor": "nx:noop"
    },
    "build": {
      "executor": "nx:noop"
    }
  }
}
